# Dashboard (Vite). In dev this is unused because vite.config.ts proxies /api
# to the local backend on :3030. In production (Vercel) set this to the Railway
# backend URL so cross-origin fetches land in the right place.
# VITE_API_URL=https://pain-points-api.up.railway.app
